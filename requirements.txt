# Terka — Python dependencies
# Install: pip install -r requirements.txt

# STT — Speech to Text
faster-whisper>=1.0.0

# LLM — Ollama client
ollama>=0.4.0

# Audio — recording and playback
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# TTS — Kokoro (optional fallback, only needed if TTS_BACKEND = "kokoro")
kokoro-onnx>=0.3.0
scipy>=1.11.0
